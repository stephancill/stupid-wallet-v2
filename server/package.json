{
  "name": "stupid-wallet-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stupid Wallet backend Cloudflare Worker: installation authentication, notification enrollment, webhook ingestion, event feed, and APNs fanout.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.10.0",
    "better-sqlite3": "^11.7.0",
    "oxlint": "^0.15.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
